{% extends 'base.html.twig' %}
{% block content %}


{{dump(book)}}

{{ form_start(form) }}
<div class="float-md-left">
    {{ form_widget(form.qty) }}
</div>
<div class="float-md-left">
    {{ form_widget(form.save) }}
</div>
{{ form_end(form) }}
{% endblock %}